import 'package:flutter/material.dart'; // Importation du framework Flutter

class AjouterDocumentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Ajouter un Document")), // Barre avec le titre
      body: Center(
        child: Text("Formulaire d'ajout d'un document ici"), // Texte temporaire
      ),
    );
  }
}
