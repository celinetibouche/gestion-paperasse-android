import 'package:flutter/material.dart'; // Importation du framework Flutter

class ListeDocumentsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Liste des Documents")), // Barre avec le titre
      body: Center(
        child: Text("Affichage de la liste des documents ici"), // Texte temporaire
      ),
    );
  }
}
